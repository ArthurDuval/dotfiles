general {
	colors = true
	interval = 5
	separator = ""
}

order += "volume master"
order += "tztime local"

volume master {
	format = "♪ %volume"
	format_muted = "♪ muted"
	device = "default"
	mixer = "Master"
	mixer_idx = 0
}

tztime local {
	format = "| %Y-%m-%d | %I:%M %p "
}
