general {
	colors = true
		interval = 5
		separator = ""
}

order += "volume master"
order += "tztime local"

volume master {
	format = "♪ %volume"
		format_muted = "♪ muted (%volume)"
		device = "default"
}

tztime local {
	format = "| %Y-%m-%d | %I:%M %p "
}
